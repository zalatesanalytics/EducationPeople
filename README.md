# EducationPeople MEAL Dashboard
AI-enabled M&E dashboard built by Zalates Analytics.

## Run locally
pip install -r requirements.txt
streamlit run app.py

## Deploy to Streamlit Cloud
Upload repo ? Run app.py
